import React from 'react';

class UserSettings extends React.Component {
  constructor(props) {
    super(props)
  };

  render() {
    return (
      <h4>User Settings</h4>
    );
  };
};

export default UserSettings;
